version: '2'
services:
  boleto-api:
    build: 
      context: ..
      dockerfile: ./devops/Dockerfile
    volumes:
      - ~/boletodb/upMongo://C/boleto_json_store
      - ~/boleto_cert://C/cert_boleto_api
    ports:
     - "3000:3000"
